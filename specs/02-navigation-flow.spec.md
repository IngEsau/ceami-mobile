# Navegación

`Login -> Home -> Nueva solicitud -> Intro -> Documentación -> Wizard (4 pasos) -> Firma -> Solicitud recibida`. El listado puede abrirse desde Home o desde Success y cada registro navega a `ApplicationDetail` mediante su id. Perfil, configuración y visitas son placeholders funcionales.
