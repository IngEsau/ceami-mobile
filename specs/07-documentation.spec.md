# Documentación

Se requieren INE frente, INE reverso y selfie. El selector de imagen usa Expo Image Picker; en entorno web o sin permiso se permite una captura mock para no bloquear la demo. El avance requiere 3/3.
