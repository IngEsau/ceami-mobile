# Documentación

Se requieren INE frente, INE reverso y selfie. La pantalla usa `useDocumentCapture`, que llama al caso de uso de captura y al puerto `DocumentCaptureSource`; el adapter Expo Image Picker puede sustituirse por cámara o KYC. En entorno sin permiso se permite una URI mock para no bloquear la demo. El avance requiere 3/3.
