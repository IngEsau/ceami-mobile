# Resultado y listado

El envío cambia el estado a `submitted` y la interfaz presenta “Solicitud recibida” / “En revisión”. El listado muestra folio, cliente, fecha, estado y progreso; tocar un registro abre su detalle local para consultar documentación, formulario y firma.
