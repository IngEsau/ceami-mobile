import React from 'react';
import { Text, View, StyleSheet, Pressable } from 'react-native';
import { NativeStackScreenProps } from '@react-navigation/native-stack';
import { RootStackParamList } from '../../../app/navigation/types';
import { AppHeader, AppScreen, BrandHeroCard, styles as ui } from '../../../shared/ui';
import { colors, radius, spacing } from '../../../shared/theme';
import { useApplicationStore } from '../application/application.store';
type Props = NativeStackScreenProps<RootStackParamList, 'ApplicationStart'>;
export const ApplicationStartScreen = ({ navigation }: Props) => { const createDraft = useApplicationStore((state) => state.createDraft); const start = async () => { await createDraft(); navigation.navigate('ApplicationIntro'); }; return <AppScreen><AppHeader title="Nueva solicitud" showBack onBack={() => navigation.goBack()} /><BrandHeroCard eyebrow="CEAMI" title="Inicio del proceso" subtitle="Comienza una nueva solicitud de forma guiada." /><Text style={styles.heading}>Selecciona una opción para continuar</Text><Pressable style={styles.option} onPress={start}><Text style={styles.optionIcon}>＋</Text><View style={styles.copy}><Text style={styles.optionTitle}>Iniciar nueva solicitud</Text><Text style={ui.secondary}>Crear un nuevo registro desde cero</Text></View><Text style={styles.arrow}>›</Text></Pressable><Pressable style={styles.option} onPress={() => navigation.navigate('ApplicationsList')}><Text style={styles.optionIcon}>▭</Text><View style={styles.copy}><Text style={styles.optionTitle}>Ver solicitudes existentes</Text><Text style={ui.secondary}>Consultar registros previos</Text></View><Text style={styles.arrow}>›</Text></Pressable></AppScreen>; };
const styles = StyleSheet.create({ heading: { color: colors.textPrimary, fontSize: 28, lineHeight: 36, fontWeight: '800', marginBottom: spacing.xl }, option: { backgroundColor: colors.surface, borderWidth: 1, borderColor: colors.border, borderRadius: radius.xl, padding: spacing.lg, flexDirection: 'row', alignItems: 'center', marginBottom: spacing.lg }, optionIcon: { color: colors.secondaryNavy, backgroundColor: '#EFF3FF', borderRadius: radius.lg, width: 64, height: 64, textAlign: 'center', textAlignVertical: 'center', fontSize: 30 }, copy: { flex: 1, paddingHorizontal: spacing.lg }, optionTitle: { color: colors.textPrimary, fontSize: 18, fontWeight: '800', marginBottom: spacing.xs }, arrow: { color: colors.textSecondary, fontSize: 38 } });
